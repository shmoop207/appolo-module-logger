export enum Level {
     'error' = 'error', 'warn' = 'warn', 'info' = 'info', 'debug' = 'debug'
}


export enum LevelIndex  {
    trace =  10,
    debug = 20,
    info = 30,
    warn = 40,
    error = 50,
}
